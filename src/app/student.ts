// create a student class with attributes id, name, age, gender, email, average_grade
export class Student {
    id!: number;
    name!: string;
    age!: number;
    gender!: string;
    email!: string;
    average_grade!: number;
}